<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="fill_parent"
              android:layout_height="wrap_content"
              android:orientation="horizontal"
              android:background="@color/colorPrimary"
              android:weightSum="6" >

    <!-- back button -->

    <Button
        android:id="@+id/titlebar_backButton"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_weight="1"
        android:background="@android:drawable/ic_dialog_email" />

    <!-- filename -->

    <TextView
        android:id="@+id/titlebar_name"
        android:layout_width="0dip"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginLeft="3dip"
        android:layout_marginRight="5dip"
        android:layout_weight="3"
        android:ellipsize="end"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:scrollHorizontally="true"
        android:singleLine="true"
        android:text="hhahahhaha"
        android:textColor="@color/colorAccent"

        android:textSize="18sp" />

    <!-- sign start -->

    <RelativeLayout
        android:id="@+id/annotation_start"
        android:layout_width="0dip"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_weight="1"
        android:background="@android:drawable/ic_dialog_email" >


        <ImageView
            android:id="@+id/titlebar_annotation_start"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:background="@android:drawable/ic_dialog_email"
            />

        <ImageView
            android:id="@+id/titlebar_annotation_finish"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:background="@android:drawable/ic_dialog_email"
            android:visibility="gone" />
    </RelativeLayout>

    <!-- more -->

    <Button
        android:id="@+id/titlebar_more"
        android:layout_width="0dip"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_weight="1"
        android:background="@android:drawable/ic_dialog_email" />

</LinearLayout>