<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.hsf.mypullToRefresh">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.seekBarActivity">
        </activity>
        <activity android:name=".activity.MeiTuanRefreshActivity">
        </activity>
        <activity android:name=".activity.SeekBarActivity2">
        </activity>
        <activity android:name=".activity.AutoHomeRefreshActivity">
        </activity>
        <activity android:name=".activity.SeekBarActivity3">
        </activity>
    </application>

</manifest>